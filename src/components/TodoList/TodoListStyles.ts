import styled from "styled-components";
import { SimpleGrid } from "@mantine/core";

export const StyledTodoWrapper = styled(SimpleGrid)`
  row-gap: 1.8rem;
  width: 80%;
  max-width: 50rem;
  min-width: 30rem;
  padding: 0;
`;
